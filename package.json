{
  "name": "mini-games",
  "private": true,
  "version": "0.0.0",
  "description": "A tiny collection of web games (static).",
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  }
}
